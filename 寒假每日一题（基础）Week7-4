#include<bits/stdc++.h>

using namespace std;

int n;
const int MAX=1005;
int a[MAX];

int main(){
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    int res=INT_MIN;
    for(int i=0;i<n-1;i++){
        res=max(res,abs(a[i]-a[i+1]));
    }
    cout<<res;
    return 0;
}
